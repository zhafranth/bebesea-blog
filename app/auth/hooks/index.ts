import { signIn } from "next-auth/react";
// import { useMutation, useQuery } from "react-query";

// export const useLogin = () => {
//   return useMutation(
//     async (payload: { username: string; password: string }) =>
//       await signIn("credentials", {
//         ...payload,
//         redirect: true,
//         callbackUrl: "/transaksi?status=1",
//       })
//   );
// };
